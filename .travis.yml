language: java
jdk:
- oraclejdk8
sudo: required
before_cache:
- rm -rf $HOME/.m2/repository/com/pingconsole
install:
- mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
cache:
  directories:
  - $HOME/.m2